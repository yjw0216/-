{
    // ctrl + B => 빌드 구성 > tasks.json생성 > others 선택
    "version": "2.0.0",
    "tasks": [
        {
            "label": "python 실행",
            "type": "shell",
            "command": "python",
            "args": [
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}